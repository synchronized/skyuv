# skyuv 协作约定

## 项目定位

skyuv 使用 libuv 替换 Skynet 的 Linux 专属网络与平台能力，使 Skynet 能在 Windows、macOS 和 Linux 上运行，同时保留原生 Actor 模型、消息队列、服务调度和上层 Lua API。

## 修改边界

- `3rd/` 存放 Skynet、libuv 等第三方源码，原则上不得直接修改。
- 必须修改第三方代码时，优先通过 skyuv 适配层完成；无法适配时，以独立且可重复应用的补丁维护，并说明原因。
- skyuv 自有实现放在 `src/`，公共接口放在 `include/skyuv/`。
- libuv 负责跨平台 I/O、事件通知及适合的平台原语，不得用它重写 Skynet 的 Actor 调度模型。
- 网络层与线程、锁等平台层应保持清晰边界，避免平台条件编译扩散到业务无关代码。

## 构建要求

- 使用 CMake 作为唯一的项目构建入口。
- 新增源码、测试或依赖时，必须同步更新相应的 `CMakeLists.txt`。
- 每个 skyuv 自有编译目标都必须调用 `skyuv_target_compile_warnings(<target>)`；不得将该警告策略应用到 `3rd/` 目标。
- 严格警告与警告即错误默认开启。新增代码不得通过全局关闭警告或降低警告等级来绕过构建错误。
- 平台差异优先封装在 `src/platform/`，仅在适配层使用 `_WIN32`、`__APPLE__` 等平台宏。
- 不提交生成目录、编译产物、IDE 用户配置或本机依赖路径。
- 不在构建脚本中假设固定盘符、用户目录或特定生成器。

## C 代码风格

整体风格与 Skynet 保持接近，并以仓库根目录的 `.editorconfig` 和 `.clang-format` 为机械格式化依据。

- 使用 C11；除第三方代码外，不引入 C++。
- C 源码和头文件使用制表符缩进，制表符宽度为 4。
- 函数与控制语句的左花括号放在同一行。
- 关键字与左括号之间留空格，例如 `if (...)`；函数调用不留空格，例如 `foo(...)`。
- 指针星号靠近变量名，例如 `struct context *ctx`。
- 类型和函数使用 `snake_case`；宏和枚举常量使用 `UPPER_SNAKE_CASE`。
- 内部符号优先使用 `static`，对外符号使用 `skyuv_` 前缀。
- 头文件使用明确的 include guard，并仅暴露必要接口。
- 注释解释设计原因、并发约束和所有权，不复述代码；项目自有代码注释使用中文。
- 不进行与当前任务无关的大规模格式化，尤其不得格式化 `3rd/`。

## 并发与资源管理

- 明确记录对象所有权、线程归属和生命周期。
- 不假设回调一定运行在调用线程；libuv handle 的创建、使用和关闭必须遵守所属 loop 的线程约束。
- 共享状态应说明使用的锁或原子内存序；不要以 `volatile` 代替同步机制。
- 错误路径必须释放已取得的资源，关闭流程应允许安全重复调用或明确禁止重复调用。
- 不把平台原生句柄泄漏到公共接口，除非该接口明确属于平台扩展。

## 测试与验证

- 修复缺陷时应增加能够复现问题的测试。
- 新增平台抽象时至少测试初始化、正常路径、错误路径和关闭流程。
- 涉及并发、唤醒或网络生命周期的修改，应包含压力或重复执行测试。
- 提交前至少完成 CMake 配置、项目构建和相关测试；无法覆盖的平台需在变更说明中明确标注。

## 文档与提交

- README 只描述已经确定的目标、用法和状态，不把设想写成已实现能力。
- 用户可见说明、项目自有文档和代码注释使用中文；公开 API 名称保持英文。
- 提交应聚焦单一目的，不混入第三方升级或无关格式调整。
- 每轮产生仓库修改并验证通过后，直接提交并推送到当前上游分支；纯讨论或无文件变化时不创建空提交。
